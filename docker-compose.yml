services:
  back:
    restart: unless-stopped
    build:
      context: ./back
      dockerfile: Dockerfile
    env_file:
      - ./back/.env
    volumes:
      - ./back/media:/app/media
      - /data:/app/data
  front:
    restart: unless-stopped
    build:
      context: ./front
      dockerfile: Dockerfile
    env_file:
      - ./front/.env

  caddy:
    restart: unless-stopped
    container_name: caddy
    image: caddy
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./caddy-config:/config
      - ./caddy-data:/data
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./back/media:/peralta/media
      - ./caddy-log:/log
